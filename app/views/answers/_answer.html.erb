

  <li>
      <%= answer.title%>

        <%= link_to t('edit'), edit_answer_path(answer) %> |
        <%= link_to t('nav.back.answers'), question_path(@question) %>

        <%= button_to t('delete'), answer,
                                             method: :delete,
                                             form: {
                                               data: { confirm: t('confirm')}
                                             }%>
</li>
