
<table class="table">
  <thead>
    <tr>
      <th scope="col"><%=t('question')%></th>

      <th scope="col"><%=t('.email')%></th>
    </tr>
  </thead>

  <tbody>
    <% @gists.each do |gist| %>
    <tr>
      <td><%= link_to "#{gist.question.body}",gist.gist_url %></td>
      
      <td><%= gist.email %></td>

    </tr>
    <% end %>
  </tbody>

</table>
